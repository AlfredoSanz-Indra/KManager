package es.alfred.kmanager.view.page.tasking.sections

/**
 * @author Alfredo Sanz
 * @date 2025
 */
enum class TasksStateModeEnum(val stateMode: Int) {
        NEW_TASK(1),
        UPDATE_TASK(2)
    }